# Geliştirme bağımlılıkları (lint, typecheck, test, deneyimsel ML araçları)
# Kullanım: pip install -r requirements-dev.txt
#
# Bu dosya, üretim için gereksiz fakat geliştirme/araştırma akışı için
# faydalı olan paketleri içerir. Üretim kurulumu için sadece
# requirements.txt yeterlidir.

-r requirements.txt

# ── Test ──
pytest-cov>=4.0.0,<6.0
pytest-xdist>=3.5.0,<4.0

# ── Lint & Format ──
ruff==0.15.12

# ── Type Check ──
mypy==1.19.1
types-PyYAML>=6.0,<7.0
types-requests>=2.31.0,<3.0

# ── Güvenlik ──
bandit[toml]>=1.7.0,<2.0

# ── Pre-commit ──
pre-commit>=3.0.0,<5.0

# ── Profiling (opsiyonel) ──
memory-profiler>=0.61.0,<1.0

# ── Experiment tracking (opsiyonel — trainer.py kodu try/except korumalı) ──
mlflow>=2.10.0,<3.0

# ── Bioinformatics extras (opsiyonel — VCF/FHIR/3D viz pipeline'ları) ──
vcfpy>=0.13.0,<0.14
stmol>=0.0.9,<0.1
py3Dmol>=2.0.0,<3.0

# ── REST API + load testing (opsiyonel — variant-gnn-api Docker servisi için) ──
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
python-multipart>=0.0.32,<1.0
locust>=2.24.0,<3.0
