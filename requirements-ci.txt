# CI ortamı için minimal bağımlılıklar (test + lint + typecheck)
# CI'da PyTorch CPU wheels ayrıca kurulur (bkz. .github/workflows/ci.yml)

# ── Test ──
pytest==8.4.2
pytest-cov>=4.0.0,<6.0

# ── Lint ──
ruff==0.15.12

# ── Type Check ──
mypy==1.19.1
types-PyYAML>=6.0,<7.0

# ── Güvenlik ──
bandit[toml]>=1.7.0,<2.0

# ── Temel ML (PyTorch hariç — CI'da ayrı adımla kurulur) ──
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
scipy==1.13.0
joblib==1.4.2
xgboost==2.0.3
lightgbm==4.3.0
imbalanced-learn==0.12.3
shap==0.45.1
pydantic==2.7.1
pyyaml==6.0.1

# ── Görselleştirme / açıklanabilirlik / tuning / rapor ──
# Çekirdek src modülleri bunları MODÜL-SEVİYESİNDE import eder; pytest COLLECTION'ı
# (src.inference/src.training/src.evaluation/src.explainability import zincirleri) bunlar
# olmadan ImportError verir. streamlit HARİÇ (UI; yalnızca smoke testi import eder ve
# dep yoksa SKIP eder — bkz. tests/smoke/_skip_if_optional_missing).
matplotlib==3.8.4
seaborn==0.13.2
networkx==3.3
tqdm==4.66.4
lime==0.2.0.1
optuna==3.6.1
fpdf2==2.7.9
