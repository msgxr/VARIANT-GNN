# GPU (CUDA 11.8) ortamı için bağımlılıklar
# Kullanım: pip install -r requirements-gpu.txt
# CUDA sürümünüze göre extra-index-url'yi değiştirebilirsiniz.

# ── CUDA 11.8 için PyTorch ──
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.2.0+cu118
torchvision==0.17.0+cu118

# ── PyG GPU wheels ──
# https://data.pyg.org/whl/torch-2.2.0+cu118.html
torch-scatter==2.1.2
torch-sparse==0.6.18
torch-geometric==2.6.1

# ── Geri kalan bağımlılıklar (sabitlenmiş ana set) ──
-r requirements.txt
