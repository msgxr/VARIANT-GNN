version: '3.8'

services:
  variant-gnn:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - STREAMLIT_SERVER_PORT=8501
    restart: always

  # Optional: Redis for caching or session management (Future)
  # redis:
  #   image: "redis:alpine"
  #   ports:
  #     - "6379:6379"

# Optional: Prometheus/Grafana for monitoring (Phase 5)
