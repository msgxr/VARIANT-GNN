# VARIANT-GNN — TEKNOFEST 2026 üretim bağımlılıkları (yarışma çıkarımı için minimal set)
# Versiyonlar reproducibility için sabitlenmiştir (§7.5 jüri kod re-run uyumu).
#
# Bu dosya YALNIZCA üretim/jüri-inference için gereken paketleri içerir.
# Geliştirme araçları (mlflow, fastapi, locust, dev paketleri) için:
#     pip install -r requirements-dev.txt

# ── Core data + ML ──
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
scipy==1.13.1
joblib==1.5.3
xgboost==2.1.4
lightgbm==4.6.0
imbalanced-learn==0.12.4

# ── Deep learning ──
torch==2.8.0
torch-geometric==2.6.1

# ── Explainability ──
shap==0.49.1
lime==0.2.0.1

# ── Visualization ──
matplotlib==3.9.4
seaborn==0.13.2
networkx==3.2.1
tqdm==4.67.3

# ── Hyperparameter Optimization ──
optuna==4.7.0

# ── Test & QA (smoke test paketi için minimal pytest) ──
pytest==8.4.2

# ── Streamlit UI ──
streamlit==1.50.0

# ── Validation (Pydantic v2 — config schema, data contracts) ──
pydantic==2.12.5
pyyaml==6.0.3

# ── Reporting ──
fpdf2==2.8.4
