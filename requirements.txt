# VARIANT-GNN — TEKNOFEST 2026 ana bağımlılıkları
# Versiyonlar reproducibility için sabitlenmiştir (§7.5 jüri kod re-run uyumu)

# ── Core data + ML ──
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
scipy==1.13.1
joblib==1.5.3
xgboost==2.1.4
lightgbm==4.6.0
imbalanced-learn==0.12.4

# ── Deep learning ──
torch==2.8.0
torch-geometric==2.6.1

# ── Explainability ──
shap==0.49.1
lime==0.2.0.1

# ── Visualization ──
matplotlib==3.9.4
seaborn==0.13.2
networkx==3.2.1
tqdm==4.67.3

# ── Hyperparameter Optimization ──
optuna==4.7.0

# ── Test & QA ──
pytest==8.4.2
pytest-cov>=4.0.0,<6.0
pytest-xdist>=3.5.0,<4.0

# ── Streamlit UI ──
streamlit==1.50.0

# ── Validation ──
pydantic==2.12.5
pyyaml==6.0.3

# ── Reporting ──
fpdf2==2.8.4

# ── Bioinformatics (opsiyonel, ortam-bazlı) ──
vcfpy>=0.13.0,<0.14
fhirpy>=2.0.0,<3.0
stmol>=0.0.9,<0.1
py3Dmol>=2.0.0,<3.0

# ── Experiment Tracking (opsiyonel) ──
mlflow>=2.10.0,<3.0

# ── CI / Quality ──
mypy==1.19.1
ruff==0.15.12

# ── API & Load Testing (opsiyonel) ──
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
python-multipart>=0.0.9,<1.0
locust>=2.24.0,<3.0
