# Quarantine — DO NOT use in PDR/jury deliverables

Files here are broken or superseded artifacts kept only for audit history.

- `external_validation_report.json` — a stale `/tmp/sample_train.csv` smoke run with
  threshold_used=0.01, ROC-AUC=0.105 (worse than random), all-positive predictions,
  MCC=0.0. It is NOT a valid external-validation result and must never be cited.
  Canonical numbers live in `RESULTS_CANONICAL.json`.
