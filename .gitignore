__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
dist_wheeltest/
build/
.eggs/
*.whl

# Sanal Ortamlar
venv/
.venv/
.verify_venv/
env/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/

# Loglar
*.log
app.log
logs/

# Model ağırlıkları — TEKNOFEST §7.5: jürinin tekrar çalıştırabilmesi için
# eğitilmiş modeller repoya DAHİL EDİLİR (<7MB toplam). Yedek/arşiv klasörleri hariç.
models/*.pth
models/*.pkl
models/*.json
models/*.txt
models/autoencoder.pth
# Yedek model klasörleri (NDA — gerçek veri türevi, lokal kalsın)
models_backup_*/
models_archive_*/
models_pre_*/
models_safety_*/
# İstisna: belge + metadata + EĞİTİLMİŞ MODEL AĞIRLIKLARI (§7.5 reprodüksiyon)
!models/PROVENANCE.json
!models/README.md
!models/manifest.json
!models/metadata.json
!models/threshold.json
!models/panel_thresholds.json
!models/gnn_arch.json
!models/ensemble_config.json
!models/preprocessor.pkl
!models/ensemble.pkl
!models/meta_learner.pkl
!models/calibrator.pkl
!models/ood_detector.pkl
!models/gnn_model.pth
!models/dnn_model.pth
!models/xgb_model.json
!models/lgbm_model.txt

# Word geçici dosyaları
~$*.docx

# Raporlar (üretilmiş çıktılar)
reports/*.png
reports/*.csv
reports/*.html
reports/*.pdf
reports/*.json
reports/*.bak
# İstisna: PDR için referans olan eğitim raporları repoda kalsın
!reports/cv_report.json
# UNTRACKED 2026-06-20 (repo hijyeni): leakage-öncesi/geri-çekilmiş (0.8980/0.9269) — jüri-repodan çıkarıldı, §IX
# !reports/cross_panel_eval.json
!reports/seed_stability.json
# !reports/ablation_report.json   (UNTRACKED 2026-06-20 — _SUPERSEDED baseline 0.898; bkz. üstteki not)
!reports/benchmark_comparison.json
# !reports/threshold_analysis.json   (UNTRACKED 2026-06-20 — eski θ=0.30, production_f1=0.9269; bkz. üstteki not)
!reports/conformal_results.json
!reports/shap_group_contributions.json
!reports/conformal_coverage_report.json
# !reports/generalization_analysis.json   (UNTRACKED 2026-06-20 — Test F1=0.9269 anomali analizi, geri-çekildi)
!reports/label_quality_report.json
!reports/ensemble_diversity.json
!reports/acmg_alignment.json
!reports/uncertainty_profile.json
!reports/feature_stability.json
!reports/panel_cross_generalization.json
# Sızıntısız retrain kanıt raporları (RESULTS_CANONICAL + dökümanlar atıf yapıyor)
!reports/leakage_quantification.json
!reports/preprocessing_diagnostic.json
!reports/dann_lopo_validation.json
!reports/bio_feature_ablation.json
!reports/threshold_consistency.json
!reports/optuna_tuning.json

# Eğitim logları
train_log.txt

# Resmi Q&A transkript artefaktları — YEREL doğrulama kanıtı (organizatörün kayıt
# yeniden-paylaşım çekincesi nedeniyle public repoya PUSH EDİLMEZ; ekip içi referans)
docs/qa/

# İç strateji/rekabet dokümanları (candid zayıflık + "iddia etme" notları içerir —
# jüriye görünen repoya GİRMEZ; PDR yazımı için ekip içi referans)
docs/strategy/

# Claude Code — kişisel ayarlar ve geçici klasörler ignore; skills + context repoda
.claude/settings.local.json
.claude/settings.json
.claude/memory/
.claude/scheduled_tasks.lock
.claude/rehber/

# DVC (Data Version Control) — projede kullanılmıyor
.dvc/
.dvcignore

# Geçici dosyalar
/tmp/
temp/

# Ortam değişkenleri
.env
.env.local
.env.*.local

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Python cache
__pycache__/
*.pyc
*.pyo

# Büyük veri dosyaları (gerçek yarışma verisi repoya eklenmemeli — TEKNOFEST NDA)
data/pretrain_100k.csv
data/raw/
data/train_variants.csv
data/train_variants_aug.csv
data/test_variants.csv
data/test_variants_blind.csv
data/jury_*.csv
submission/predictions.csv

# submission/ büyük çıktılar
submission/teknofest/*.zip
submission/teknofest/*.pdf
submission/archive/

# DVC stage outputs — sadece .dvc pointerlar repo'da kalır
# (Gerçek veri LFS/DVC remote'ta olmalı)
.dvc/cache/
.dvc/tmp/

# Test ve Lint Cache
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/

# Notebook çıktıları
*.ipynb
!docs/**/*.ipynb

# Streamlit cache (config.toml hariç — jüri tekrar-çalıştırma için tema sabitlenir, §7.5)
.streamlit/*
!.streamlit/config.toml

# Büyük yarışma dosyaları (NDA gereği repoda olmamalı)
*.docx
# create_*.py — scripts/altina tasindi
markdown_to_word.py
VARIANT_GNN_*.docx

# Locust & benchmark çıktıları
reports/locust_results*.csv
reports/benchmark_results.json

# macOS genişletilmiş
__MACOSX/
*.DS_Store

# Büyük scriptler (geçici)
generate_report_pdf.py
build_notebook.py

# Dürüst SHAP §4.4 görselleri (PDR kanıtı)
!reports/shap_summary.png
!reports/shap_waterfall.png
!reports/figures/shap_group_contributions_honest.png
!reports/figures/conformal_coverage.png
!reports/ensemble_weight_justification.json
!reports/figures/ensemble_weight_justification.png
!reports/lever_test.json
!reports/oof_per_model.json
!reports/weight_optimization.json
!reports/weight_scheme_test.json
!reports/oof_per_model.npz
!reports/balanced_weights.json
!reports/stacking_improvement.json
!reports/threshold_deep.json
!reports/balanced_jury_f1.json
!reports/competition_jury_f1.json
!reports/panel_model_comparison.json
!reports/pah_analysis.json
!reports/missing_indicator_ablation.json
